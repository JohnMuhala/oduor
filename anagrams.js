//--directions
// check to see if two provided strings are anagrams ore eachother.
//one string is an anagram of anlother if it uses the same characters
//if the same quality.Only consider characters not spaces 
//---Examples 
 //  anagrams ('raile safety', 'fairy tales',)--> true


 let stringA = 'rail safety'
 let stringB = 'fairy tales'

 function anagrams(stringA, stringB){
     let strA = my_string(stringA === stringB)
     let strB = my_string(stringB === StringA)

     if(stringA ===stringB)
     {console.log('true')}
     else if{
         console.log('true')

     }else{
         console.log('false')
     }
 }